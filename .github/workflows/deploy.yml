name: deploy
on:
  push:
    branches: ["main"]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: build-artifact
          path: dist/
      - name: Deploy (demo)
        run: |
          echo "Simulating deployment..."
          ls -al dist
          echo "✅ Deployment step complete (demo)."
